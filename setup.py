from setuptools import setup, find_packages

setup(name='pyg-pretrained',
      version='0.1',
      description='Pretrained GNN models',
      author='Montai Health',
      author_email='cme@montai.com',
      url='https://www.python.org/sigs/distutils-sig/',
      packages=find_packages(),
     )